import { BlurrableDirective } from './blurrable.directive';

describe('BlurrableDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurrableDirective();
    expect(directive).toBeTruthy();
  });
});
